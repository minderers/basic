# 变更日志

## [1.0.0] - 2025-10-31

### 新增
- 实现统一日志工具类 `Logger`
  - 封装 HarmonyOS `hilog` 系统
  - 支持 `debug`、`info`、`warn`、`error` 四个日志级别
  - 提供简洁易用的 API 接口
- 实现字符串处理工具类 `StringUtils`
  - 支持生成 UUID v4
  - 提供 Base64 编解码功能（基于 `@ohos.util`）
  - 支持正则校验：邮箱、中国大陆手机号
- 实现日期处理工具类 `DateUtils`
  - 支持时间戳格式化（如 `"yyyy-MM-dd HH:mm:ss"`）
  - 提供 `now()` 获取当前时间戳
  - 实现 `lunarToSolar(year, month, day)` 农历转公历功能（基础版本，适用于常见场景）
- 实现数学计算工具类 `MathKit`
  - 支持生成指定范围内的随机整数
  - 提供数值格式化功能（支持千分位分隔符和小数位控制）
  - 包含常用三角函数工具：角度 ↔ 弧度转换
- 初始化项目结构
- 添加基础文档（README、LICENSE）

### 修复
无

### 变更
无

### 其他
- 首次发布版本